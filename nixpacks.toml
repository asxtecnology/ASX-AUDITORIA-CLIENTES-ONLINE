[phases.setup]
aptPkgs = ["chromium", "fonts-liberation", "libnss3", "libatk-bridge2.0-0", "libdrm2", "libxkbcommon0", "libgbm1"]

[variables]
PUPPETEER_EXECUTABLE_PATH = "/usr/bin/chromium"
CHROME_PATH = "/usr/bin/chromium"
